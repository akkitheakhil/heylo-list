import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { BoardingScreenData } from "src/screens/onboarding/OnBoardingScreen";

const HeyloSwiperOnboarding = ({ data }: { data: BoardingScreenData }) => {
  return (
    <View>
      <Text>HeyloSwiperOnboarding</Text>
    </View>
  );
};

export default HeyloSwiperOnboarding;

const styles = StyleSheet.create({});
